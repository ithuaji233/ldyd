# 29 站长插件部署说明

本文档是交给站长的实际部署说明。  
站长拿到插件目录后，按本文档操作即可完成安装、配置、验证和日常使用。

## 0. 我方实际地址

对接到我方时，当前使用的正式地址如下：

- 插件网关地址：`http://orix.ld-yd.com`
- 站长后台登录地址：`https://mira.ld-yd.com`

说明：

- 插件里的 `gateway_base_url` 必须填写：`http://orix.ld-yd.com`
- 不要把站长后台地址 `https://mira.ld-yd.com` 填到插件配置里
- `mira.ld-yd.com` 是站长网页登录后台
- `orix.ld-yd.com` 才是插件实际请求的对接网关

## 1. 你会拿到什么

我方交付给站长的内容通常有 4 项：

- `php-plugin` 插件目录
- 一组站长后台账号
- 一组站点密钥
- 一份已填写或待填写的 `config.local.php`

其中：

- 站长后台账号：用于登录站长后台查看余额、订单、工单、密钥申请
- 站点密钥：用于插件调用我方对接网关

## 2. 部署前先确认

你的宿主站需要满足下面条件：

- 网站是 PHP 环境
- 宿主站存在 `confing/common.php`
- 宿主站登录后，`common.php` 内能得到：
  - `$islogin`
  - `$userrow`
- PHP 已启用：
  - `curl`
  - `sqlite3`
- 插件目录有写入权限

如果你的站点不满足上面这些条件，就不是直接上传即用，需要先做宿主适配。

## 3. 插件目录怎么放

把我方给你的 `php-plugin` 整个目录上传到你网站目录里。

例如你的网站根目录原本是：

```text
网站根目录/
```

那你可以把插件放成：

```text
网站根目录/plugin-station/
```

也可以改成别的目录名，例如：

```text
网站根目录/29-plugin/
```

建议直接使用：

```text
plugin-station
```

这样后续交接最省事。

## 4. 插件目录结构说明

插件目录里主要会看到这些文件：

- `index.php`
  - 插件根入口，会自动跳到插件页面
- `index/plugin.php`
  - 插件前端页面入口
- `api.php`
  - 插件接口入口
- `cron.php`
  - 定时同步入口
- `config.php`
  - 默认配置
- `config.local.php`
  - 站长自己的实际配置
- `lib/bootstrap.php`
  - 宿主站登录态接入、本地订单库、权限校验
- `lib/client.php`
  - 请求我方网关
- `public/app/`
  - 插件前端静态资源
- `data/`
  - 插件本地 SQLite 数据目录

## 5. 第一步：准备配置文件

如果插件目录里没有 `config.local.php`，就把：

```text
config.local.example.php
```

复制为：

```text
config.local.php
```

然后编辑 `config.local.php`。

标准示例：

```php
<?php

return [
    'plugin_name' => '二九运动对接插件',
    'gateway_base_url' => 'http://orix.ld-yd.com',
    'site_id' => '我方分配给你的site_id',
    'app_key' => '我方分配给你的app_key',
    'app_secret' => '我方分配给你的app_secret',
    'site_name' => '你的站点名称',
    'db_path' => __DIR__ . '/data/plugin-local.sqlite',
    'host_root' => '',
    'host_common_path' => '',
    'cron_token' => '请替换为随机长字符串',
];
```

## 6. 第二步：配置项怎么填

### 6.1 必填项

#### `gateway_base_url`

这是我方给你的对接网关地址。  
例如：

```php
'gateway_base_url' => 'http://orix.ld-yd.com',
```

当前正式值就是：

```php
'gateway_base_url' => 'http://orix.ld-yd.com',
```

这一项就是插件对接我方的真正入口。  
学校、平台、价格、下单、查状态、工单等请求，都会先打到这个 `gateway`。

#### `site_id`

这是我方给你的站点编号。

#### `app_key`

这是我方给你的插件调用密钥。

#### `app_secret`

这是我方给你的插件调用密钥密文。

这 4 项填错任何一个，插件都会连不上我方服务。

### 6.2 常用项

#### `plugin_name`

插件页标题名称。  
这个可以改成你自己想显示的名字。

#### `site_name`

你自己的站点名称，用于插件内和对接链路展示。

#### `db_path`

插件本地 SQLite 数据库位置。  
一般保持默认即可：

```php
'db_path' => __DIR__ . '/data/plugin-local.sqlite',
```

### 6.3 宿主站路径项

#### `host_root`

如果插件目录放在网站根目录下的一个子目录里，一般填这个即可。

例如你的网站根目录是插件目录的上一级，那么写：

```php
'host_root' => dirname(__DIR__),
```

#### `host_common_path`

如果你的 `confing/common.php` 不在默认位置，就直接填写它的完整相对位置关系对应的真实路径。

`host_root` 和 `host_common_path`：

- 二选一即可
- 优先推荐 `host_root`
- 只有宿主结构比较特殊时才用 `host_common_path`

### 6.4 定时同步项

#### `cron_token`

如果你要通过浏览器 URL 或宝塔计划任务访问同步接口，就必须设置一个随机长字符串。

例如：

```php
'cron_token' => 'b6a4d0d2f5c1491cb5c6e72f7f0f8d18',
```

## 7. 第三步：确认插件静态资源完整

插件前端不是外链页面，而是已经打包在插件目录里的。

你要确认目录里存在：

```text
public/app/.vite/manifest.json
public/app/assets/*.js
public/app/assets/*.css
```

如果这些文件不存在，页面会提示“插件前端资源尚未构建”。

正常交付包里这些文件应该已经带好了，站长不需要自己编译。

## 8. 第四步：给插件目录写权限

插件运行时会在 `data/` 目录写本地 SQLite 数据。

所以你要确保下面目录可写：

```text
插件目录/data/
```

如果这个目录不可写，常见现象是：

- 下单后本地订单写不进去
- 状态同步异常
- 工单或订单列表不正常

## 9. 第五步：打开插件页面

部署完成后，访问：

```text
/插件目录/
```

或者：

```text
/插件目录/index/plugin.php
```

例如插件目录名是 `plugin-station`，那就是：

```text
/plugin-station/
```

如果能正常看到插件页面，说明前端资源和 PHP 入口都已经接上了。

## 10. 第六步：检查登录态接入是否正常

插件依赖宿主站当前登录用户。

所以正确的测试顺序是：

1. 先登录你的宿主站账号
2. 再打开插件页面

如果插件提示：

```text
请先登录站点账号
```

说明插件已经接到宿主站，但当前用户未登录。

如果你明明已经登录宿主站，插件还提示未登录，那就是宿主登录态接入没成功，需要检查：

- `confing/common.php` 是否正确加载
- `$islogin` 是否为 `1`
- `$userrow` 是否正常存在
- `host_root` / `host_common_path` 是否配置正确

## 11. 第七步：怎么验证安装成功

安装完成后，至少做下面 6 项验证。

### 11.1 页面能打开

访问插件页面能正常显示。

### 11.2 学校能加载

下单页里的学校列表能正常出来。

### 11.3 平台能加载

选择学校后，平台列表能正常出来。

### 11.4 价格能加载

选择学校和平台后，能看到正常价格。

### 11.5 能正常下单

用测试账号提交一笔订单，订单能进入待审核或已同步状态。

### 11.6 站长后台能看到订单

登录我方给你的站长后台，应该能看到：

- 自己的站点
- 自己的密钥
- 自己的余额
- 自己的订单

## 12. 站长后台怎么用

我方会另外给你一组站长后台账号。

当前站长后台地址是：

```text
https://mira.ld-yd.com
```

站长后台主要用于：

- 查看站长余额
- 查看订单
- 查看密钥
- 查看站点
- 提交密钥申请
- 查看工单和处理状态

站长后台不是部署在你的网站里，而是我方单独提供的后台地址。

再次强调：

- 插件配置里填写的是：`http://orix.ld-yd.com`
- 站长网页登录的是：`https://mira.ld-yd.com`

## 13. 插件怎么日常使用

站点用户的正常流程是：

1. 用户先登录你自己的宿主站
2. 打开插件页面
3. 选择学校、平台
4. 填写账号密码和公里数
5. 提交订单
6. 插件把订单送到我方对接站
7. 订单进入主站审核链路
8. 状态再回流到插件页面

插件页面里通常会用到这些功能：

- 查价
- 下单
- 查状态
- 查看订单
- 提交工单
- 查看工单回复

## 14. 定时同步怎么配置

为了保证订单状态及时回流，建议配置定时同步。

### 14.1 命令行方式

优先推荐用命令行计划任务：

```bash
php 插件目录/cron.php
```

### 14.2 URL 方式

如果只能用 URL，则使用：

```text
/插件目录/api.php?action=cron_sync&cron_token=你设置的token
```

建议每 1 到 5 分钟执行一次。

## 15. 常见问题排查

### 15.1 页面空白或提示前端资源未构建

检查：

- `public/app/.vite/manifest.json` 是否存在
- `public/app/assets/` 下的 js、css 是否存在

### 15.2 插件提示未登录

检查：

- 宿主站是否真的已登录
- `confing/common.php` 是否能拿到 `$islogin`
- `confing/common.php` 是否能拿到 `$userrow`
- `host_root` 或 `host_common_path` 是否填对

### 15.3 能打开页面但学校列表加载失败

检查：

- `gateway_base_url` 是否填错
- `site_id`、`app_key`、`app_secret` 是否填错
- 服务器是否能访问我方对接网关

### 15.4 下单失败

检查：

- 当前宿主站用户余额是否足够
- 站长后台余额是否足够
- 插件目录 `data/` 是否可写
- 网关地址和密钥是否正确

### 15.5 状态不回流

检查：

- 是否配置了 `cron.php` 定时任务
- `cron_token` 是否正确
- 插件本地数据库是否正常写入

## 16. 交付时我方需要提供给站长的最终信息

每个站长最终应拿到下面这份信息：

### 16.1 插件包

- `php-plugin`

### 16.2 插件配置

- `gateway_base_url`：`http://orix.ld-yd.com`
- `site_id`
- `app_key`
- `app_secret`
- `site_name`
- `cron_token`

### 16.3 后台账号

- 站长后台地址：`https://mira.ld-yd.com`
- 站长后台用户名
- 站长后台密码

## 17. 推荐交付方式

推荐交付给站长时，直接提供：

- 插件目录
- 一份已经填好的 `config.local.php`
- 站长后台账号
- 本文档

这样站长只需要：

1. 上传目录
2. 放到自己站点里
3. 检查写权限
4. 打开页面
5. 配置定时同步

就能开始使用。
