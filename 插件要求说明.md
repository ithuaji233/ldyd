# 插件要求说明

## 一、目标

牢大运动插件必须对标“服务器版杀手运动”交付形态，做到：

- 一个独立目录就是完整插件
- 解压即可部署
- 只改一个 `config.php`
- 改完即可运行
- 入口固定、清晰
- 关键运行逻辑全部收口在插件目录内

目标形态必须是：

1. 上传压缩包
2. 解压到站点
3. 修改 `laoda/config.php`
4. 直接访问入口

不允许出现“还要去找目录外源码、目录外构建产物、目录外脚本、目录外说明文件才能运行”的情况。

## 二、交付标准

插件交付后，`/laoda` 本身就必须是完整可运行目录，至少应包含以下内容：

```text
/laoda
├── index.php
├── api.php
├── config.php
├── index/
│   ├── plugin.php
│   └── laoda.php
├── public/ 或 js/
├── monitor/
│   ├── monitor.php
│   └── monitor_daemon.php
├── lib/
└── 其它插件自身运行所需文件
```

要求如下：

- `index.php` 为根入口
- `api.php` 为统一接口入口
- `config.php` 为唯一集中配置文件
- `index/plugin.php` 或 `index/laoda.php` 为固定后台/页面入口
- `public/` 或 `js/` 内直接放可运行前端产物
- `monitor/` 内放监控、同步、状态刷新等脚本
- `lib/` 内放插件内部逻辑

## 三、配置要求

配置必须集中到一个 `config.php`。

必须满足：

- 站长只改 `laoda/config.php`
- 禁止依赖 `config.local.example.php`
- 禁止依赖 `config.local.php`
- 禁止二段式、三段式配置
- 禁止要求站长再去别的项目目录补配置

也就是说，交付后的插件不能再出现：

```text
先复制 config.local.example.php
再改 config.local.php
再改别处环境变量
再编译前端
再同步脚本
```

这种形态不符合要求。

## 四、入口要求

插件入口必须固定、清晰，像现成插件一样直接访问，不需要额外解释。

要求如下：

- 杀手运动是：`/index/ydsj.php`
- 牢大运动应保持：`/index/laoda.php` 或 `/index/plugin.php`
- 根目录可保留 `index.php` 作为统一跳转入口

总之，站长拿到插件后，应当一眼就知道访问哪个入口，而不是还要看文档找路径。

## 五、前端要求

前端如果使用 Vue 或其它前端工程，最终交付时必须已经编译完成，并放回插件目录内。

要求如下：

- 不允许交付后还依赖目录外前端源码
- 不允许交付后还要求站长自己执行构建
- 不允许运行时再去引用别的项目构建目录
- 最终运行所需静态资源必须直接存在于 `laoda/public/` 或 `laoda/js/`

也就是说，插件压缩包上传后，里面看到的就应该是可直接运行的前端产物，而不是源码工程。

## 六、监控与同步要求

凡是插件运行依赖的监控、同步、刷新、状态更新能力，都必须放在插件目录内。

要求如下：

- 监控脚本放入 `laoda/monitor/`
- 例如：
  - `monitor/monitor.php`
  - `monitor/monitor_daemon.php`
- 不允许把关键同步逻辑散落到别的项目目录
- 不允许部署时还依赖“去别处找一个定时脚本”

插件目录本身就必须具备完整的运行闭环能力。

## 七、禁止事项

以下做法一律不符合插件交付要求：

- 插件目录不完整，需要依赖目录外源码
- 插件目录不完整，需要依赖目录外构建产物
- 插件目录不完整，需要依赖目录外监控脚本
- 配置不集中，必须改多个文件
- 入口不固定，还要额外解释访问路径
- 交付后还要求站长自行编译前端
- 关键逻辑散落在别的项目中
- 压缩包解压后不能直接配置运行

## 八、推荐交付口径

牢大运动插件今后的标准交付口径应统一为：

```text
上传压缩包
解压到站点
修改 laoda/config.php
访问 /index/laoda.php 或 /index/plugin.php
```

这才叫：

- 解压
- 改配置
- 直接访问

## 九、当前建议

牢大运动插件后续所有开发、重构、发布、打包，都必须围绕下面这条原则执行：

**一个独立目录就是完整插件。**

如果某项改动会导致：

- 需要再补目录外文件
- 需要再补目录外配置
- 需要再补目录外构建产物
- 需要再补目录外脚本

那么这种改动就不应进入最终交付版本。
